# Hilfe-zur-Pflege — Milestone / Change Log

> Running log of shipped changes, in enough detail that a future Claude Code session
> or a new engineer can understand the current state of the product **without**
> reverse-engineering it from git history. Complements `docs/architecture.md`
> (the source-of-truth design doc; its §7 lists the original M1–M5 milestones).
> Newest entries at the top.

---

## Current state snapshot (as of 2026-07-05)

Quick orientation for anyone picking this up cold:

- **Milestone status:** M1–**M6** complete; **M7 (go-live stabilisation + ops bridge) code/docs complete 2026-07-21 with HUMAN-OPEN items** — three UAT cases, the founder pilot, and the Roman items (confirm-signup template = pilot-blocking; Pankow PLZ list) must be run/closed before M7 and go-live are called complete (see the M7 section). RLS audit done (50/50). M6 = dynamic document requirements (2026-07-21); M5 = secure document upload + Pankow rules (2026-07-20); M4 = Berlin content pass. Domain cutover to `sorglosantrag.de` deferred — pilot on the vercel.app URL; checklist in `docs/operations.md` §6. PDF generation stays parked.
- **Document uploads (M5):** rules live only for **Sozialamt Berlin-Pankow** (`11000000-…-0001`; 18 Pankow PLZs at priority 20, list pending Roman) — `document_catalog` (30, `DOC-####`) + `office_document_rule` (50, `PAN-###`, JSONB conditions) seeded by the committed `scripts/generate_pankow_rules_seed.py`; evaluated by `lib/document-rules.ts` into per-person slots after completion. Files live in the **private `case-documents` bucket** (15 MB, pdf/jpeg/png/heic/heif, owner-RLS on `storage.objects`), metadata in `document_upload`; uploads go browser→storage via server-minted signed upload URLs; downloads via 60-second signed URLs only. Essen document rules arrive later as a pure data drop.
- **Questionnaires (prod):** Berlin (`30000000-…-0001`, office Sozialamt Berlin, 9 categories / 158 questions), **Essen** (`30000000-…-0003`, office Sozialamt Essen `10000000-…-0162`, 8 categories / 9 groups / 248 questions — originally GENERATED by `scripts/generate_essen_seed.py` from `essen_hzp_question_master_v3.xlsx` — both **frozen as the historical seed record** since content pass 3; content changes go through dated migrations only), fallback (`…-0002`, **deactivated** since CP3). **Routing:** `resolvePlzAction` maps PLZ → office → that office's active questionnaire; PLZs resolving to no questionnaire get the **Berlin questionnaire** and proceed normally (CP3/D12) — adding a questionnaire for an office needs zero `postal_code_rule` changes.
- **Live app:** https://sorglos-antrag.vercel.app — Next.js 16 + Supabase (EU), Vercel prod region `fra1`.
- **Deploy:** push to `main` on GitHub (`BerkHakcil/Sorglos_Antrag`) → Vercel auto-deploys prod. Vercel team `berk-solutions`, project `sorglos-antrag`.
- **DB migrations:** dated SQL in `supabase/migrations/`, applied to prod with **`supabase db push`** (the CLI is linked; it records each version in `supabase_migrations.schema_migrations`). **Never** apply content via the Supabase dashboard/Studio (see reminders). DDL cannot be run from the Claude Code sandbox — the co-founder runs `db push` after reviewing each migration.
- **Questionnaire is data-driven** (see `architecture.md` §3): categories → questions (some in repeatable `question_group`s) → `question_option`s; answers keyed `(case_id, question_id, group_instance)`.
- **Current category flow order (Berlin)** (`category.sort_order`): `antragsteller` (0) → `wohnsituation` (1) → `einkommen` (2) → `kinder` (3) → `income` (4) → `expenditure` (5) → `wealth` (6) → `additional` (7) → `spouse` (8). Essen: `antragsteller → wohnsituation → kinder → income → expenditure → wealth → additional → spouse` (no `einkommen`; sections per the Essen master).
- **Fresh-case required-question count (progress denominator):** Berlin **57**, Essen **50** (was 49 before content pass 3: −1 maintenance bulk, +1 maintenance select, +1 Ausland yes/no, children gate net ±0) for a single applicant before answering `marital_status`.
- **Repeatable groups** (`question_group.is_repeatable = true`, all uncapped `max_count = NULL`): `children` (Kinder), `pension`, `other_income`, `bank_additional`, `additional_wealth`, plus the spouse mirrors `spouse_pension`, `spouse_other_income`. Since Tier 7 a group can carry a DB-authored loop-prompt override (`question_group.custom_prompt_de`); when NULL the UI falls back to the `"Möchten Sie eine weitere {group} hinzufügen?"` template.
- **Verification tooling:** Playwright **MCP** works (needed `npx @playwright/mcp install-browser chrome-for-testing` once) and the **e2e runner** (`npx playwright test`, bundled Chromium). **Signup smoke check:** `npm run smoke:signup` proves the auth-email pipeline (signup → SMTP acceptance → cleanup); run pre-release and after any SMTP/Brevo change. Live structural checks use an adaptive-loop e2e spec driven against prod; throwaway test users are created via the admin API and deleted in `finally`. **Seed-drift guard:** `scripts/verify-baseline.mjs` diffs prod against a fresh local migration replay (`supabase db start` + `supabase db reset`, then run with `LOCAL_DATABASE_URL`) across **all** seeded tables — questionnaire content plus `care_home`, `social_office`, `postal_code_rule`, `questionnaire`, `static_content`. Run it after any `migration repair` backfill.

### Migrations added in this pass (all applied + tracked on prod)

| File                                                           | Tier  | Effect                                                                                                                                                                    |
| -------------------------------------------------------------- | ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `20260703000001_fix_spouse_dependent_visibility_not_value.sql` | 1     | spouse Sonderstatus/life-insurance-amount rules `value:"Ja"` → `not_value:"Nein"`                                                                                         |
| `20260703000002_static_content.sql`                            | 2     | new `static_content` table + 6 header/footer strings                                                                                                                      |
| `20260704000001_reorder_front_split_name.sql`                  | 4     | split name; reorder front of `antragsteller`; move address block in                                                                                                       |
| `20260704000002_tier5_categories.sql`                          | 5     | new `wohnsituation`; rename `dokumente`→`kinder`; delete `familienstand`                                                                                                  |
| `20260704000003_tier6_additional_wealth_repeatable.sql`        | 6     | make `additional_wealth` a repeatable group                                                                                                                               |
| `20260705000001_tier7_content_pass.sql`                        | 7     | country dropdown + default; Betreuer options (optional); year dropdowns; Mietfrei; insurance reorder; IBAN copy; "Keine Rente"; `custom_prompt_de` + 4 loop prompts       |
| `20260705000002_feedback_pass2.sql`                            | FP2   | real care homes (drift heal) + test-case cleanup; `birth_name` + opening reorder; `last_residence_plz` deleted; Betreuer "Nein"; children loop prompt; locked-banner copy |
| `20260705000003_month_year_enum.sql`                           | Essen | `answer_type` + `'month_year'` (own migration — enum-use rule)                                                                                                            |
| `20260705000004_essen_questionnaire.sql`                       | Essen | GENERATED: full Essen questionnaire (8 categories, 9 groups, 248 questions, 460 options); zero Berlin/routing statements                                                  |
| `20260705000005_roman_review_strings.sql`                      | Roman | spouse bank-field prompts + six spouse group loop prompts (both questionnaires)                                                                                           |
| `20260711000001_cp3_structural.sql`                            | CP3   | D1-D12 structure: children gate, Unterhalt select, Hausrat/cash/funeral-due/Ausland restructures, member_since type, fallback questionnaire deactivated                   |
| `20260711000002_cp3_copy_berlin.sql`                           | CP3   | GENERATED: 131 Berlin prompts + 49 placeholders from docs/content/berlin_hzp_copy_changes.csv                                                                             |
| `20260711000003_cp3_copy_essen.sql`                            | CP3   | GENERATED: 182 Essen prompts + 60 placeholders + 38 umlaut sweeps from docs/content/essen_hzp_copy_changes.csv                                                            |
| `20260711000004_m5r1_berlin_questionnaire.sql`                 | M5    | D1 3-value person-2 triggers; D2 spouse bank structure + group; D3 citizenship gates                                                                                      |
| `20260711000005_m5r2_office_tables.sql`                        | M5    | Sozialamt Berlin-Pankow + 18 PLZ rules; document tables + RLS; legacy M1 document tables dropped; 12 docs.\* static keys                                                  |
| `20260711000006_m5r2_document_rules.sql`                       | M5    | GENERATED: 30 catalog docs + 50 Pankow rules (key map + D3 re-targets)                                                                                                    |
| `20260711000007_m5r2_storage_bucket.sql`                       | M5    | private case-documents bucket (15MB, 5 mime types) + storage.objects owner policies                                                                                       |

(All 15 migrations that existed before Tier 4 were backfilled into `schema_migrations` — see Tier 2. The three Tier 4/5/6 migrations were applied via `supabase db push`, which tracks them automatically.)

---

## Content pass — Roman's answers (A+B) + pending-German ledger finalization — 2026-07-23, **verification pending `db push`**

**A) Pankow PLZ expansion (`20260723000002`):** Roman approved all 18 seeded PLZs and added three — **10247, 10249, 13051** (his reply listed 10247 twice; deduped, final set = 21 unique). Same pattern as the original 18: single-PLZ ranges, priority 20, overriding the generic priority-1 rules. ⚠ Deliberate per Roman's policy, not an error: 10247/10249 are administratively Friedrichshain, 13051 Lichtenberg. **Routing policy recorded verbatim in `docs/operations.md` §7** (include-when-in-doubt; duplicate routings resolved when the overlapping office is implemented; missed PLZ degrades to default questionnaire + default checklist; wrong-office arrivals notified back = accepted super edge case) and referenced in known-limitations.

**B) Grammar fix (Roman):** Berlin `spouse_bank_giro` → "Bei welcher Bank hat Ihr Partner **ein** Girokonto?" (was "sein"; same migration; the only question carrying the old wording).

**Pending-German ledger — FINALIZED BY FOUNDER DECISION (2026-07-23).** Rationale: the strings have been live for weeks through three Roman review passes with zero flags = implicit review. **Finalized:** all docs-area UI strings + M6 counter strings + tab labels (C); confirm-signup email template + both subject lines + `emailSendFailure` (D — the former "blocking for pilot" label is removed everywhere: this entry, the M7 HUMAN-OPEN table, known-limitations); spouse `member_since` + spouse Ausland prompts (E1/E2); section-B items 1, 2, 4–7 (Roman-blessed verbatim this week). **Closed by founder decision:** E4 unsupported-PLZ notice (permanently none — reinforced by the routing policy), F1 spouse funeral (not doing unless Roman's Essen rules file raises it), F2 vehicles-repeatable (backlog). **Remaining open Roman items — exactly three:** the **Essen document rules file** (the one real deliverable; governs the live default checklist), and the **spouse-section review** carrying only E3 (bulk-intro alignment) + the insurance-depth question — both cosmetic, non-gating.

**M7 closure check:** with D finalized and the PLZ list confirmed, both Roman rows in the M7 HUMAN-OPEN table are marked satisfied. Genuinely open there now: **only the founder rows** — UAT Cases 1–3 and the founder pilot. The founder makes the M7-complete call.

---

## Mini content pass — Berlin CP3 parity (children gate + Bargeld) — ✅ complete 2026-07-23

Founder decision: the Essen-only CP3 scoping is superseded now that Berlin serves as the universal fallback questionnaire.

**Children gate (migration `20260723000001`):** Berlin got the exact Essen shape — flat required `children_yes_no` ("Haben Sie Kinder?", Ja/Nein, fixed ids for replay determinism) at the top of `kinder`, members renumbered 1–8, `child_first_name` gated `{value:"Ja"}` (the other 7 members already chain off it via `not_empty`). **Denominators unchanged (±0): fresh 53, married 92** — gate +1, `child_first_name` hidden fresh −1, exactly the Essen precedent. **ADDITIVE BACKFILL (new pattern of record):** three real completed Berlin cases held child answers; the migration backfilled `children_yes_no="Ja"` for exactly those cases (guarded INSERT..SELECT — no-op on local replay, idempotent), so their children stay visible and their locked 100% progress intact. Nothing deleted; the trio is additionally covered by the 07-22 snapshots. **Verified live (4/4):** gate-Nein full drive — 53 fresh, completed, zero `child_*` answers; gate-Ja drive — child questions appeared, full instance (8 answers) captured, completed; **fixture replica** of a migrated case (pre-gate child answers + backfilled Ja, never the real cases) — gate shows answered and the child instance still renders in history; Essen untouched — 50 fresh, opening prompt unchanged. verify-baseline full replay: **all 12 tables identical** (questions 413, options 1016).

**Bargeld: closed as already-parity.** Berlin has no cash yes/no+amount pair — only the single unconditional required `cash_savings` ("Wie viel Bargeld haben Sie?"), which IS the post-D7-merge shape, word-identical to Essen's. No migration. Founder call: "haben" stays (the task's "besitzen" wording was descriptive, not new copy — pending-Roman pro forma).

---

## Feedback pass 3 (pilot review, ClickUp) — ✅ complete 2026-07-22

Six founder items, two-phase (Phase-1 diagnosis report approved with calls; single push round of six migrations after code deploy `14c66b3`).

**Verified live on prod (8/8 tests green after the push):** L1–L4 — Berlin AND Essen driven to completion under **ledig and verwitwet**, DB-proof of **zero spouse/Partner questions asked** (a completed case answers every visible required question, so zero spouse-ish answer rows ⇒ none were shown); Berlin fresh denominator **53** confirmed, Essen 50. T1 — Dokumente tab + badge visible **before any answer**, mandatory slots listed, spouse slots + larger badge appeared **LIVE** after saving marital=verheiratet (no reload), pre-completion upload succeeded with case status untouched (`in_progress`), badge visible at 375px. R1 — Essen case completed and used the **Pankow-default checklist (12 slots — the never-fire under-collection working as classified)** with a successful upload; R2 — fallback case at 53 with the tab present. documents-m6 married regression re-passed fully under the tab layout (spec adapted: area lives in the tab pane; completion signal = the "In Prüfung" chip). verify-baseline full replay: **all 12 seeded tables identical** (questions 414→412; `app_config` added to the drift guard). All test users/objects cleaned with loud-failure logging. DB spot-checks: junk spouse answers deleted (only the verheiratet case's 2 rows remain), rules/placeholders/prompts as migrated.

1. **Spouse-question leak (Berlin-only):** `spouse_health_insurance_amount` + `spouse_care_insurance_amount` had **no visibility rule at all** (NULL) — asked of and required from every user; they even sat in every denominator. Systematic sweep (chain-walker over 67 Berlin + 112 Essen spouse-ish questions, modeling the engine's not*value-matches-unanswered semantics): exactly these 2 leaks; Essen clean — its 9 flagged `ex_partner*\*`/`maintenance`rows are the deliberate CP3 Unterhalt design, excluded. Fix`20260722000001`: the sibling 3-value marital gate on both + deletion of the junk answers the completion-blocking leak forced from non-partnered cases (pre-push audit: 4 rows, 2 real cases — both snapshotted first; the partnered case's rows kept). **Pending Roman:** whether Berlin should also mirror Essen's insurance-type gating depth (freiwillig/privat) on these amounts.
2. **"Fragen | Dokumente" tabs from first login:** the checklist is no longer completion-gated — `CaseTabs` client switcher (both panes stay mounted; chat state survives switching), missing-count badge on the Dokumente tab, full-height panes on mobile. Live recompute: `chat-view` now `router.refresh()`es after every successful save, so conditional slots appear/disappear mid-questionnaire (same mechanism uploads already used). Uploads pre-completion allowed — the M5 upload path never checked status (M6 independence extends). Tab labels "Fragen"/"Dokumente" pending-Roman pro forma.
3. **Default document rules = Pankow (founder decision, supersedes D5):** new `app_config` table (`default_document_office_id`, migration `20260722000002`) — a case whose office has zero rules (and the office-less unsupported-PLZ case) uses the default office's rules; an office with rules always uses its own; no default → no tab (safety branch). One migration flips the default to Essen when Roman's seed lands. **Evaluator absent-key semantics verified correct as-built** (equals/not_equals/any/all never match absent keys; explicit unit tests added). **Cross-questionnaire compatibility** (approved table): 19 marital-gated rules gained an `equals "Lebenspartnerschaft"` branch (Essen's option value; Berlin-inert) and PAN-020 gained Essen's two life-insurance values (`20260722000003`, GENERATED); **PAN-022 excluded by its stop-rule** — `funeral_insurance_yes_no` exists in Berlin too, so the proposed branch was not Berlin-inert; Essen keeps never-fires for that rule. 11 rules keyed on Berlin-only questions silently never fire for Essen cases (accepted under-collection); Pankow-specific mandatory slots now show for everyone (accepted over-collection) — both recorded in known-limitations.
4. **child_profession placeholder** "Kauffrau" → "Pflegefachkraft, Rentner, etc." in both questionnaires (`20260722000004`).
5. **Berlin `rent_heating` + `rent_warm_water` DELETED** (`20260722000005`) — both were unconditional+required with zero dependents. REAL-DATA audit: 3 answers each across 3 cases, **all real** — per the snapshot-first precedent all three cases were `case:export`ed before the push (66/107/64 answer rows + files archived locally). **Denominators: Berlin fresh 57 → 53, married 94 → 92** (−2 from this deletion, −2 from item 1's gating); Essen unchanged at 50. UAT doc + regression asserts updated.
6. **berlin_since / berlin_district_since** reworded verbatim per Roman (`20260722000006`) — rationale: Berlin is the universal fallback questionnaire; Berlin-specific wording confused non-Berlin users.

New suite `tests/e2e/feedback-pass.spec.ts`: L1–L4 (Berlin+Essen × ledig+verwitwet full drives — completion + DB-proof of zero spouse-ish answers) and T1 (tabs from first login, spouse slots appearing LIVE on the marital save, pre-completion upload with status untouched, badge at 375px). Married regression = the unchanged `documents-m6` suite. Flipped asserts in `m7-regression` (Essen/fallback now expect the default checklist; fallback denominator 53).

---

## M7 — go-live stabilisation + operations bridge — code/docs complete 2026-07-21, **HUMAN-OPEN items below**

Two-phase milestone (audit+plan → confirmed execution). Domain cutover to `sorglosantrag.de` **deferred by decision** — the pilot runs on `https://sorglos-antrag.vercel.app`; the ready-to-execute cutover checklist (apex-primary + www-redirect confirmed as target shape) is `docs/operations.md` §6, and the e2e `BASE` constants are now `E2E_BASE_URL`-overridable so cutover is a one-line default flip.

**RLS + access audit (Phase 1): 50/50 PASS, zero critical findings.** Behavioral proof on prod with two real authenticated users + anon key: all cross-user reads empty and writes rejected on every runtime table, role self-escalation rejected (profiles WITH-CHECK pin), storage fully isolated (download/signed-URL/list/upload/delete against a foreign prefix all fail), anon surface = zero rows on all 16 public tables + storage denied + public object URLs 400, service key server-only. GDPR deletion cascades the full DB chain (profile→case→answers→upload metadata→status events, verified) but **storage objects orphan — confirmed**; the deletion runbook therefore orders storage-prefix cleanup BEFORE user deletion (`docs/operations.md` §4). **Log PII audit:** 4 server-side console sites, none log user data (signup logs GoTrue code+message only — never the attempted email); zero client console output; zero third-party scripts; no error monitoring by decision (Vercel/Supabase logs only).

**Shipped:** `npm run case:export -- <case_id>` (`scripts/case-export.mjs`) — the operations bridge: `answers.md` (German Q&A in questionnaire order incl. repeatable-group instances), `documents.md` (evaluated checklist via the SAME `lib/document-rules.ts` evaluator, per-slot upload status), `files/` (all uploads via service role); `exports/` gitignored (personal data), verified against a live fixture case. `docs/operations.md` (lookup, export, correction policy, GDPR deletion, logs/incidents, cutover checklist), `docs/known-limitations.md` (consolidated from the triage — quirks #4/5/6/8/9/10 + email/app-domain mismatch as pre-scale item), `docs/uat-m7.md` (3 UAT cases + founder-pilot appendix, targeting the vercel.app URL). New condensed regression `tests/e2e/m7-regression.spec.ts` (Essen full drive → NO document area; unmapped-PLZ fallback), complementing the Pankow leg (`documents-m6.spec.ts`) and `smoke:signup`.

**M7 verification evidence (all on prod):** smoke:signup HTTP 200 green; Pankow leg fully green (all six M6 acceptance criteria re-passed incl. counter strings); Essen + fallback regression green after three SPEC-side fixes (zero product bugs): (1) the generic driver lacked a `long_text` branch — Essen stalled at 56/57 (the denominator legitimately grows from 50 as gates open); (2) the 66606 assert expected `unsupported` where CP3 semantics correctly write `resolved` — 66606 matches the St.-Wendel rule and the Berlin fallback comes from the office-without-questionnaire path; `unsupported` is only for no-rule PLZs; (3) the completion signal is the "In Prüfung" status chip — the locked-banner text is DB-authored static_content since FP2 and not a stable test anchor (the M6 spec never noticed because Pankow's document area doubles as its terminal signal). One environment flake (`ERR_NETWORK_IO_SUSPENDED` mid-run) surfaced a cleanup gap: a network outage also kills the afterEach delete and the old `.catch(() => {})` swallowed it silently, leaking the user — all three adaptive-loop specs now log cleanup failures loudly; the one leaked user was found and deleted.

**Triage decisions of record (founders):** confirm-signup template+subjects = blocking for pilot, with Roman (dashboard-side) — **superseded 2026-07-23: finalized by founder decision, live copy = final**; Pankow PLZ list = required before pilot (migration only if Roman's answer changes it); document-area/counter copy review non-blocking; quirks documented not fixed; stale-user cleanup via dashboard by founder; `anomis55@gmail.com` held pending Roman's confirmation it was his failed attempt.

### HUMAN-OPEN before M7 / go-live can be called complete

| Item                                                                                                                    | Owner      | Status                                                                                    |
| ----------------------------------------------------------------------------------------------------------------------- | ---------- | ----------------------------------------------------------------------------------------- |
| UAT Case 1 — Pankow married, 2 pensions, phone incl. HEIC (`docs/uat-m7.md`)                                            | founders   | ☐ open                                                                                    |
| UAT Case 2 — Essen full drive, no document area                                                                         | founders   | ☐ open                                                                                    |
| UAT Case 3 — unmapped PLZ 66606 → Berlin                                                                                | founders   | ☐ open                                                                                    |
| Founder pilot — real case to under_review + `case:export` handed to Roman ("PDF fillable from export" = the acceptance) | founders   | ☐ open                                                                                    |
| Confirm-signup email template + both subject lines                                                                      | Berk+Roman | ✅ satisfied 2026-07-23 — finalized by founder decision (ledger sweep: live copy = final) |
| Pankow PLZ list confirmation (migration only if it changes)                                                             | Roman      | ✅ satisfied 2026-07-23 — 18 approved + 3 added (`20260723000002`)                        |

---

## M6 — dynamic document requirements — ✅ complete 2026-07-21

Light milestone by design: most of the spec was already satisfied by M5's rules engine; M6 added the missing-documents counter, proved the remaining acceptance criteria explicitly on prod, and recorded the interpretation decision. Commit `e1b4c06`.

**Interpretation decision (founders, of record):** "new requirements can appear after later answers" means the **system property** — the checklist is computed live from answers on every load, never snapshotted — **not** post-completion answer editing. The M3 edit lock stands. **Product question for Roman:** should users be able to edit answers after completion (e.g. add a forgotten pension), with the document checklist updating? That reopens the M3 edit lock and needs its own scoping if wanted.

**Already satisfied by M5 (pointers):** liveness is structural — `DocumentSection` (`app/case/page.tsx`) re-runs `evaluateDocumentRules` over the current answers on every server render; nothing is materialized (the M1 `case_document_requirement` snapshot design was dropped in M5). Conditional rules, per-instance repeats and composite bank slots: `lib/document-rules.ts`. Upload independence is structural too — uploads bind `(rule_id, instance_key)` and `document-actions.ts` never touches status or answers.

**M6 added:** (1) the counter — pure `countMissingSlots()` (`lib/document-rules.ts`, unit-tested) over the server-provided slots+uploads; header shows plural/singular/complete via three new `static_content` keys (`docs.missing_count` / `docs.missing_count_one` / `docs.all_uploaded`, migration `20260721000001`). No schema change, no new computation path; upload and delete already `router.refresh()`, so it updates without a full page reload. Code deployed before the migration deliberately — missing static **rows** degrade to `''` by design (the Tier-7 ordering rule concerns new **columns**). (2) The committed acceptance suite `tests/e2e/documents-m6.spec.ts` (adaptive drive keyed by a DB-loaded prompt→key map — no brittle text heuristics). (3) The liveness proof and the two new asserts (disability-absence, spouse slot trio).

**Verified live on prod (suite green, 2.4 min):** Berlin married drive via Pankow PLZ 13187 (112 steps, 2 applicant pensions à `Altersrente`, `disability_card`=Nein) → **19 slots** (14 person_1, 5 person_2). **A1** exactly 2 applicant pension slots (`Rente 1/2: Altersrente`) — M5 re-asserted under the M6 suite. **A2** zero `Schwerbehindertenausweis` slots on Nein. **A3** spouse `Personaldokument` = 1 (PAN-002), spouse pension slot = 1 per driven spouse pension instance (PAN-004), spouse `Kontoauszuege – Girokonto` = 1 per spouse account (PAN-006 — exercising the M5-R1 spouse bank block end to end). **A4 (numeric)** initial `data-missing` = 19 = DOM missing count; upload → 18; delete → 19; all 19 uploaded → 0 (complete state); delete one → 1 (singular state). **A5 LIVENESS** on the completed case: service-role write flips `disability_card` Nein→Ja → reload → slot **appears**, count 20; flip back → slot gone, count 19. ⚠ **TEST-ONLY technique** — the flip is a service-role DB write, not a product path; answers stay locked in the product. **A6** case status `under_review` and the full answer set byte-identical after ~20 uploads/deletes (upload flow is fully independent of application state). Fixture note: the citizenship gates defaulted to Nein, so `Aufenthaltsstatus` slots (PAN-026/027) appeared for both persons — consistent with D3. Cleanup: 18 storage objects removed, bucket prefix verified empty, test user deleted.

**Post-`db push` close-out (same day):** migration `20260721000001` applied; the suite re-ran fully green **including the exact German strings** (plural with n, singular after one delete, complete state with all uploaded); verify-baseline full replay: **all 11 seeded tables identical** (static_content now 23 rows). Both runs cleaned up completely (bucket prefix empty, test users deleted).

**Pending Roman (M6):** the three counter strings — `Es fehlen noch {n} Dokumente.` / `Es fehlt noch 1 Dokument.` / `Alle erforderlichen Dokumente sind hochgeladen.` (all constructed) — plus the post-completion-editing product question above.

---

## Signup outage — Brevo SMTP auth failure — ✅ resolved 2026-07-20

**Symptom:** every prod registration failed with the generic "Registrierung fehlgeschlagen. Bitte versuchen Sie es erneut."; Supabase edge logs showed `POST /auth/v1/signup` → 500. Password reset was equally dead (same transport). **Root cause:** Brevo rejected the SMTP login — auth logs showed `535 "5.7.8 Authentication failed"` on every send; GoTrue surfaces that as 500 `unexpected_failure` / "Error sending confirmation email" and **rolls the user back**, so failed signups left no unconfirmed user behind (retries never hit "already registered"). Working SMTP was last proven 2026-07-15 (Roman's confirmed signup). **Fix (dashboard-side, per the documented exception):** new Brevo SMTP key **and** the corrected username in Supabase SMTP settings — the username must be the Brevo **SMTP login** from Settings → SMTP & API (`…@smtp-brevo.com`), not the account email; both values were wrong.

**Diagnosis technique worth keeping:** `supabase.auth.signUp` runs inside the Server Action, so the browser never sees `/auth/v1/signup` — reproduce auth failures by curl-POSTing the endpoint directly with the publishable key (identical request, full JSON error body). `POST /auth/v1/recover` for an existing user is a safe differential probe (no state change on failure; distinguishes transport-wide failure from a template-specific one — both templates failed here, proving transport).

**Hardening shipped with the incident (code):** (1) `mapSupabaseError` moved out of the `'use server'` file into `lib/auth-errors.ts` (pure, unit-tested — `'use server'` modules can only export async functions), now matching GoTrue's structured `error.code` first (`user_already_exists`, `weak_password`, `over_email_send_rate_limit`, …) with the old message substrings as fallback; (2) a **new distinct German message** for email-send failures (`signup.errors.emailSendFailure`, **PLACEHOLDER pending Roman**) instead of collapsing into the generic string; (3) `signupAction` now `console.error`s the raw code+message before mapping, so Vercel function logs name the real cause (this line missing is why the outage was opaque); (4) **signup smoke check** `npm run smoke:signup` (`scripts/smoke-signup.mjs`): POSTs a real signup, asserts HTTP 200 (GoTrue only 200s after SMTP _accepts_ the mail — exactly the assertion that fails in this incident class), deletes the throwaway via admin API in `finally`. Costs one email of the 30/hr budget; run pre-release and after any SMTP/Brevo change. It verifies acceptance, not inbox delivery — Brevo's transactional statistics cover that.

**Verified on prod after the fix:** `/auth/v1/recover` → 200; full browser signup drive → success screen, unconfirmed user + profile (names/phone/consent timestamps) + auto-created case all correct, throwaway deleted with cascade confirmed; smoke script green end-to-end.

**Observed in passing (not fixed):** 8 leftover `@hzp-test.invalid` test users (06-28/07-01) plus one unconfirmed real signup (`anomis55@gmail.com`, 07-06) still in Authentication → Users — cleanup candidates. ~~`npm run format:check` fails on 14 pre-existing files on clean main~~ **Correction (2026-07-21):** that finding was a Windows artifact, not real drift — `core.autocrlf=true` puts CRLF in the local working tree and Prettier (`endOfLine: "lf"`) flags it, while all repo content is stored LF and CI's blocking format:check (Linux, LF checkout) has been green on every push. Fixed by `.gitattributes` (`* text=auto eol=lf`) so Windows checkouts get LF too; `git add --renormalize` confirmed zero content changes.

---

## M5 — secure document upload + Pankow document rules — ✅ complete 2026-07-20

Shipped in two push rounds per plan. **Round 1** (`21de047`): the Berlin questionnaire prerequisites — D1 (person-2 triggers → 3-value marital set; separated users lose the spouse section), D2 (spouse bank structure mirroring the applicant side incl. the repeatable `spouse_bank_additional` group), D3 (citizenship yes/no gates, free-text questions gate=Nein-conditional). Verified live: fresh **57** / married **94** / separated **57** (D1 regression), Roman's spouse-bank loop prompt captured verbatim, D3 proven in both directions, Essen untouched at 50. **Round 2** (`5654ebd` + fixes `1badefe`/`49b8b58`): the document system — pure rule evaluator (`lib/document-rules.ts`, unit-tested; `always/equals/not_equals/any/all` + `repeat_for_each` with composite bank slots and Keine-Rente exclusion), signed-upload-URL flow (server authorizes + mints path `{case_id}/{upload_id}.{ext}`, browser PUTs direct to storage — Vercel's ~4.5 MB body cap forbids proxying — bucket limits authoritative, post-upload verify records metadata), document area on the case page post-completion **only when the resolved office has rules (D5)**, self-service delete (object + metadata together), 60-second signed downloads, no public URLs anywhere. `Sozialamt Berlin-Pankow` + 18 higher-priority PLZ rules (**list pending Roman**); Pankow PLZs get the Berlin questionnaire via the CP3/D12 office-without-questionnaire default. Catalog (30) + rules (50) seeded by the committed `scripts/generate_pankow_rules_seed.py` with Roman's `DOC-####`/`PAN-###` ids, the D8 key map (liablity typo, `automobile_owner`), and PAN-026/027 re-targeted to the D3 gates. Legacy M1 document tables dropped. The storage-schema migration **applied cleanly on hosted** (stop rule never triggered).

**Acceptance verification (all green, prod):** Pankow case (13187) completed → area rendered with **2 pension slots** (two pensions; a Keine-Rente instance yields none), **3 bank slots** (giro + savings + 1 additional account) and the conditional Wohngeld slot; PDF/JPG/PNG/**HEIC** uploads landed with correct case/rule/instance binding (multi-file per slot proven); `.docx` and 16 MB rejected with the German errors; **bucket-side authority proven independently** (direct signed-URL PUTs of 16 MB and a docx mime both rejected by storage); public-URL and unauthenticated fetches fail; a second authenticated user sees zero foreign metadata (RLS), cannot download or mint signed URLs for foreign paths, signed-URL guessing fails; area **absent** for Essen-routed and fallback-routed cases; upload works on the mobile viewport; delete removes object + row together. Test users deleted; **bucket verified empty**; verify-baseline (now **11 tables** incl. `document_catalog`/`office_document_rule`) replays identical.

**Findings caught by the check suites before shipping:** (1) my first Pankow office id (`10000000-…-0201`) **silently collided with the seeded Sozialamt Koblenz** via ON CONFLICT DO NOTHING — 18 Berlin PLZs would have routed to Koblenz; moved to the `11000000-…` namespace. (2) The M1 schema already owned the `document_upload` table name (old shape) — legacy tables now drop before the new ones. (3) **HEIC on desktop browsers**: Chromium reports an empty `file.type` AND supabase-js sends the File's own type on the PUT — two fixes (extension-derived MIME + re-wrapping typeless Files) or desktop HEIC uploads fail; real-user-facing, found by the acceptance fixture.

**Accepted behavior (documented, per decision):** an orphaned storage object (PUT succeeded, metadata verify never ran — e.g. tab closed mid-upload) is invisible in the area (it lists metadata only) and harmless; **no cleanup machinery by design**. D7 stands: HEIC stored as-is, download-to-view for reviewers until a conversion step exists.

### M5 kickoff (decisions of record)

**Source of truth committed:** `docs/content/pankow_document_rules_cto_master.xlsx` (`6da6170`) — canonical tabs: README (policies incl. 4-month bank-statement period), Document Catalog (30 `DOC-####` docs, office-independent), Pankow Rules (50 `PAN-###` rules with machine-readable `condition_json`: `always`/`any`/`all`/`equals`/`not_equals`/`repeat_for_each`), Question Changes (6, all resolved into decisions). Mandatory/Conditional tabs are filtered views; Legacy Mapping is historical rationale only.

**Decisions D1–D10 (confirmed):** D1 Berlin person-2 triggers shrink to the 3-value marital set (separated users lose the spouse section); D2 Berlin spouse bank structure mirrors the applicant side (integrating the two existing spouse amounts; Roman's approved loop prompt); D3 citizenship yes/no gates (both persons, Berlin; PAN-026/027 re-target them with exact matches); D4 spouse funeral contract **deferred**; D5 document area only for cases whose resolved office has rules (today: Pankow) — **superseded 2026-07-22 by the feedback-pass founder decision: every case gets the default (Pankow) checklist, from first login**; D6 area on the case page post-completion, all copy via static_content; D7 HEIC accepted stored as-is (**known limitation: download-to-view for reviewers until a conversion step exists**); D8 seed-time key mapping (never rename live keys); D9 POA "Nein" shipped in FP2 (only rule PAN-010 is new), **vehicles-repeatable deferred**; D10 standalone pure rule evaluator (not bolted onto VisibilityRule).

**Pending Roman (M5):** D2/D3 constructed German (spouse bank prompts via the "… Ihres Partners" pattern, citizenship gate wordings), D6 document-area UI copy (title, slot statuses, upload button, error messages, empty state), Confirm-Signup email template + both email subject lines (see Email delivery below). **Deferred:** D4 spouse funeral rule/question, vehicles-repeatable, Essen document rules (await Roman's data drop — pure data once M5 ships), HEIC preview.

---

## Email delivery (dashboard-managed by design)

Custom SMTP is live via **Brevo** (EU provider) with an **authenticated sending domain** (SPF/DKIM records at the registrar), sender name "Sorglos Antrag". Supabase's auth-email rate limit is now **30/hr** (adjustable in the dashboard). German templates: **Reset Password customized with Roman's copy**; **Confirm Signup is a PLACEHOLDER pending Roman**, as are **both subject lines**.

**2026-07-20 credential reset (see the signup-outage section):** the SMTP key was regenerated in Brevo and the Supabase SMTP **username corrected to the Brevo SMTP login** (`…@smtp-brevo.com`, shown in Brevo → Settings → SMTP & API — _not_ the account email). If auth emails ever fail again, run `npm run smoke:signup` first, then check the Supabase Auth logs for the raw SMTP error before touching anything.

⚠ This configuration is **intentionally outside the migration/drift-guard system** — Supabase Auth email settings and templates are dashboard state (Supabase Dashboard → Authentication → Emails / SMTP settings), and the DNS records live at the registrar / Brevo. This is the documented exception to the migrations-only rule: auth-email config cannot be expressed as SQL migrations. When it changes, update this section.

---

## Content pass 3 + routing fix — ✅ complete 2026-07-11

Commits `11b029c` (engine) + `5f5f719` (migrations) + docs; Roman's 08.07 Essen review + the two consolidated copy CSVs (committed as canonical at `docs/content/*.csv`, `3266133`). ~315 copy updates, 109 placeholders, structural changes D1–D12, and the routing fallback.

**Engine:** input placeholders via `validation.placeholder_de` (short_text/long_text/number/amount; no schema change); **skip-and-resume now works on repeatable-group members** (D13 — chat-view withheld `onSkip` for instances although the nav layer already supported compound skip keys; Roman hit this on `child_address`); **unresolved PLZs get the Berlin questionnaire** (D12: office-without-questionnaire and no-rule cases both; `plz_resolution_status='unsupported'` still written as the internal signal; the amber notice is **suppressed** — its copy promised a team follow-up, contradicting the new proceed-normally behavior). The empty "Allgemeiner Fragebogen" fallback row is deactivated (kept for old FK references). Care-home select label = Roman's Essen CSV row 1 (shared pre-questionnaire UI — Berlin users see it too).

**Structure (Essen unless noted):** children gate "Haben Sie Kinder?" (bank*additional flat-trigger pattern; Berlin children stay ungated); Unterhalt: bulk → 5-option `maintenance_claims_status` select, `ex_partner*\*` re-gated via **`in_values`of the four non-Nein options** (deliberate deviation from the not_value spec wording: identical gating once answered, but avoids +8 fresh-case denominator inflation — confirmed decision); Hausratversicherung fully removed (options + amount questions, both sides); cash merged to one unconditional Bargeld question per side; "im Ausland gelebt" bulk option → dedicated yes/no pair gating the abroad details; applicant bulk option 2 reworded (label only — **value unchanged so rules keep matching**), flag-only option 4 deleted; funeral due-date questions deleted (yes/no + amount confirmed staying);`member_since` `date`→`short_text` (docx prompts, both sides); Berlin+Essen Mietrückstände prompts per docx (D1 overrides the CSV phrasing on Euro fields).

**Copy (generated — `scripts/generate_cp3_copy.py`, committed):** Berlin 131 prompts + 49 placeholders; Essen 182 prompts + 60 placeholders + 38 umlaut sweeps (labels/prompts only — **option values never touched**; protected words like "aktuell"; `Niessbrauch→Nießbrauch` as the only ß case). The generator hard-asserts CSV Translation == live prompt; documented exceptions: Berlin remaps 24→`disablity_card_application` (DB key typo), 25/124 (CSV key typos), 90/151→`automobile_owner`s, **94/155→`automobile_holder`s (silent key mis-targets caught by the scan)**, Essen 210→`spouse_applicant_bulk_topics`, and Berlin row 84's stale pre-FP2 Translation. A replay check also caught the sweep re-applying the pre-CP3 label over D9's reworded option — structural-owned options are excluded from the sweep.

**CSV supersession (moot rows):** Essen 49 (deleted bulk), 93/183 (deleted cash yes/nos), 103/193 (deleted funeral due dates); docx overrides on Essen 38/59/162 and Berlin 43 (rent_debt). No Euro/amount field carries a Sample Answer in either CSV (the "Euro placeholder" verification point was covered by a second text field instead).

**Generator freeze (decision):** `generate_essen_seed.py` + the v3 master xlsx are **frozen as the historical seed record** (they produced `20260705000004`). All later content is owned by dated migrations; the earlier "keep generator constants in sync" practice is retired (this pass supersedes ~180 of its strings plus structure). Correctness stays guarded by the verify-baseline full replay.

**Pending Roman review (this pass):** spouse member_since prompt "Wie lange ist Ihr Partner schon Mitglied dieser Krankenversicherung?" (constructed, D1); spouse Ausland prompt "Hat Ihr Partner jemals im Ausland gelebt?" (constructed, D8); the three spouse bulk prompts (income/expense/wealth — kept as-is, umlaut-swept; the new applicant wording is subject-specific and doesn't mirror); new copy for the suppressed unsupported-PLZ notice. Still open from before: Berlin spouse section review (Roman, next week).

**Verified live on prod:** PLZ 66606 and 00001 → Berlin questionnaire loads, is answerable, **no amber notice**; 45127 → Essen, 10115 → Berlin. Essen drive: fresh denominator **"0 von 50 Fragen"**; gate Nein → maintenance select next, gate edited to Ja → child questions appear; **skip button present on a group-member long_text**; maintenance non-Nein → ex_partner appears, edited to Nein → gone; member_since as free text; bulk shows exactly 4 options incl. reworded label, exclusivity intact; placeholders live ("Jobcenter Essen", IBAN sample); funeral amount → next question is life insurance (no due date); single unconditional Bargeld; Ausland yes/no gates details; umlauts live (Übertragsverträgen/Nießbrauch, EU-Ausländer/in). Berlin smoke: **57 unchanged**, new front copy in order, "Musterstraße 12" placeholder renders, rent_debt D1 wording, two CSV spot checks, children ungated. Test users deleted. verify-baseline full replay: **all 9 tables identical** (now 403 questions).

---

## Essen questionnaire + multi-questionnaire routing — ✅ complete 2026-07-05

Commits `6d95056` (engine) + `5a8b933` (migrations A/B, generator, master xlsx) + docs. Goal proven live: **an Essen previous-residence PLZ gets a fully working Essen questionnaire; Berlin PLZs keep the Berlin questionnaire; both complete end-to-end.** PDF generation and document upload/checklists (incl. the master's "Dokumentregeln" sheet) explicitly out of scope — parked for later milestones.

**Multi-questionnaire scoping facts** (verified, not just designed): a second questionnaire is purely additive data — `question.key` has no unique constraint and every engine path (`buildRulesByKey`, `answersMap`, `deriveGroupData`, save/completion) operates on the one loaded questionnaire, so Essen reuses keys like `marital_status` freely; answers key on `question_id`. Routing needed **zero changes**: `resolvePlzAction` already resolves PLZ → office → active questionnaire, so inserting the Essen questionnaire row (office `…162`) made every Stadt-Essen PLZ (e.g. 45127, 45326) route to it. Gladbeck/Duisburg PLZs route to their own offices → still fallback, by design (routing keys off the user's previous residence, not the facility).

**Engine additions** (deployed before content, all inert for Berlin): `{includes}` visibility-rule variant for multi-select controllers (transitive visibility + the stale-answer sweep cover it unchanged — deselecting a bulk option hides AND deletes its detail answers); data-driven exclusive none-option (`validation.exclusive_value`) enforced in the checkbox component and server-side; `month_year` answer type (`<input type="month">`, stored `YYYY-MM`, shown `MM.YYYY`, 1900…year+1 bounds both sides); generic `validation.pattern` for `short_text` (IBAN/BIC/5-digit-PLZ regexes live as data); verify-baseline Questions diff re-keyed by `id` (keys now repeat across questionnaires and a key-based map would mask drift).

**Content** (migration B, generated): 8 categories (Berlin's German labels reused), **9 repeatable groups** (children, pension, other_income, bank_additional, additional_wealth + the four spouse mirrors incl. `spouse_additional_wealth`), 248 questions, 460 options, 68 `includes`-gated bulk details. Decisions baked in: D1 (7th marital option `eheähnliche Gemeinschaft`; spouse gate = verheiratet/Lebenspartnerschaft/eheähnliche Gemeinschaft), D2 (spouse bulk split into 4 — option **value** = the dependency-referenced applicant-style "Es …" string, **label** = "Der Partner …" verbatim), D4 (bank-accounts groups with name/IBAN/BIC/amount for both sides), D5 (`residence_status` unconditional), D6 (health yes/no gates: Ja → rows 58–63 detail block, foreign insurance unconditional, Nein → last-insurance block), D7 (`Keine Rente` + `in_values` of the 12 real pension types), D8 (no PLZ question — `cases.plz_before_move` remains the only source), D9 (`child_high_income_100k_yes_no` last in the children group), D10 (Roman's five loop prompts reused; spouse groups on template fallback). "Starts with Ja" dependencies became `in_values` of the two exact Ja options. The master's comma-joined option cells needed curated splits (three option strings contain commas — the Berlin generator's naive split would have corrupted them).

**Generator:** `scripts/generate_essen_seed.py` reads the committed `essen_hzp_question_master_v3.xlsx` (sheet "Question Master", 240 rows; rows 1/2 are the pre-questionnaire care-home/routing steps, row 30 the dropped PLZ, row 210 the split spouse bulk) and emits migration `20260705000004`. It hard-asserts: every `includes` target matches an option value of its controller, every referenced controller exists, keys unique, counts as expected — a master typo fails generation instead of shipping.

**Verified live on prod:** Essen married drive — 133 prompts to completion: first prompt "Wie lautet Ihr Nachname?" (Essen loads via 45127); one detail triggered from **each** bulk incl. both spouse MM.YYYY dates; exclusivity ("Nein, nichts davon" cleared a real selection); health gates both paths; "Keine Rente" → loop prompt (no gross/net) then Altersrente → gross+net asked; IBAN/BIC/PLZ patterns rejected bad input then accepted valid; pension + bank groups looped via "Ja, hinzufügen"; all five custom prompts verbatim + two spouse template prompts observed; locked banner after completion. Fresh-case denominator **49**. Berlin regression: denominator 57, opening flow byte-identical, children loop prompt unchanged. Both test users deleted. Full verify-baseline replay: **all 9 seeded tables identical** (now 17 categories / 16 groups / 406 questions / 1006 options / 3 questionnaires).

**Roman content review — RESOLVED 2026-07-05** (migration `20260705000005_roman_review_strings.sql` + code commit; all captured live on prod afterwards):

1. Health-gate wording **approved as-is** ("Sind Sie aktuell krankenversichert?" / "Ist Ihr Ehepartner / Lebenspartner aktuell krankenversichert?").
2. Spouse bank-account field prompts — Roman's mechanical rule, " Ihres Partners" before the "?": "Bei welcher Bank haben Sie ein weiteres Konto Ihres Partners?", "Was ist die IBAN Nummer dieses Kontos Ihres Partners?", "Wie lautet die BIC Ihres Partners?" (Essen ids `…f8/f9/fa`).
3. `invalidFormat` (de.ts, code — not static_content): now **"Ungültiges Format."**, matching the "Ungültiges Datum." convention.
4. Spouse group loop prompts, Roman's pattern "Möchten Sie {X} Ihres Partners hinzufügen?" — set on all four Essen spouse groups AND Berlin's two (`spouse_pension`, `spouse_other_income`; Berlin has no spouse group outside these concepts, so nothing was left on the template): "…weitere Renten…", "…sonstiges Einkommen…", "…weitere Bankkonten…", "…weitere Vermögenswerte Ihres Partners hinzufügen?".
5. Essen fresh-case denominator **49** sanity-confirmed.

**Still open:** Berlin spouse section content review — Roman is testing next week; expect a feedback pass.

**Generator contract note (important):** the committed `essen_hzp_question_master_v3.xlsx` and the original generator constants are **superseded** by migration `20260705000005` on exactly these seven strings — the three spouse bank prompts and the four Essen spouse `custom_prompt_de` values listed above (they were generator-synthetic additions, not master cells, so a future **v4 master regeneration must not resurrect the old wording**; the generator constants were synced in the same commit). And structurally: the seed migration inserts with `ON CONFLICT DO NOTHING`, so **regenerating never updates rows that already exist on prod — post-launch content edits always need explicit UPDATE migrations** like `…000005`.

**Flagged engine edge (found during this verification, not fixed):** when a repeatable group's last member is also the questionnaire's **final required question**, answering it completes the case (status → `under_review`) and the "add another?" loop prompt is never offered — the user can't add a second instance. Berlin never hits this (a flat category always follows its groups); **Essen hits it on the married path when only "sonstiges Vermögen" is selected in the spouse wealth bulk** (`spouse_additional_wealth` ends the flow). Options when it's addressed: hold the completion gate while a group prompt is pending, or ensure content never ends on a repeatable group. Needs a decision — parked.

Also inherited: the "Keine Rente" quirk (loop prompt right after selecting it) exists in Essen exactly as in Berlin — same engine behavior, same flagged status.

---

## Feedback pass 2 (post-M4 co-founder review) — ✅ complete 2026-07-05

Commits `92521e4` (migration `20260705000002_feedback_pass2.sql`) + `efc6761` (code) + the verify-baseline extension. Ten independent fixes from the co-founder's live review, same check-first-then-apply discipline.

**What shipped (items 1–10):**

1. **Consent copy** — the authorization sentence now names the company ("…und ermächtige **Sorglos Antrag**, in meinem Namen…"). Fixed in place in `de.ts` (signup page isn't wired to static_content; one-word company-name correction). All benefit references to "Hilfe zur Pflege" untouched.
2. **Signup infobox removed** — the "betreuende Person" clarification box is gone; the post-submit email-confirmation message stays.
3. **Fall-ID row removed** from the case header card (status/PLZ rows stay).
4. **Real care homes** — see the drift finding below. Dropdown now shows facility name only (address stays in the DB; the old code appended it).
5. **New opening order + `birth_name`** — new OPTIONAL question `birth_name` ("Was ist Ihr Geburtsname?", id `…f3`); `in_facility_since` (move-in date, from `einkommen` — verified: no dependents, no references) and `last_residence_street`/`_city` (from `wohnsituation`) moved into `antragsteller`. Flow now opens: `first_name → birth_name → last_name → geburtsdatum → move-in → street → city → …`. **This partially unwinds Tier 5 by deliberate co-founder decision** — `wohnsituation` keeps only `berlin_since`, `berlin_district_since`, `apartment_ownership` (renumbered 0–2).
6. **Date bounds app-wide** — `DateInput` min/max `1900-01-01`…`(current year+1)-12-31`, mirrored server-side in `validateAnswerValue` (the real gate; rejects the observed year-22000 typo). No per-question DB validation — one consistent bound.
7. **`last_residence_plz` DELETED** — it duplicated the routing PLZ captured pre-questionnaire. Verified first: controls nothing, referenced by nothing, no code path reads it; 9 pre-launch test answers cascaded. ⚠ **PDF/Antragsformular sourcing note: the previous-residence PLZ must come from `cases.plz_before_move` (set by the pre-questionnaire PlzForm), NOT from an answer row — no questionnaire question captures it anymore.** (Also recorded in the migration comment.)
8. **Betreuer/Beistand "Nein" option** — added as the FIRST option, so "confirmed: no Betreuer" is now a stored answer instead of an unanswered skip. This resolves the M4-backlog product question about distinguishing "no answer" from "confirmed none".
9. **Children loop prompt** — `custom_prompt_de = "Haben Sie weitere Kinder?"` (Tier 7 mechanism, one UPDATE).
10. **Locked-banner copy → static_content** — the under-review banner (`EditLockedCard`) is now DB-authored via new keys `case.locked_heading`/`case.locked_body` (heading + body layout; de.ts string removed). Values are currently identical to `case.all_answered_*` by the co-founder's choice, but kept as separate keys so the two states stay independently editable.

**Care-home drift finding (the important one):** prod was still serving the **7 fake Frankfurt facilities** while the repo's seed migration (`20260614000001`) had long listed the 7 real ones under the same UUIDs. Root cause: the Tier 2 `supabase migration repair` backfill **stamped the seed as "applied" when it had never actually run against prod** — repair trusts the operator and registers history without verifying content. **Lesson: a backfilled migration needs content verification, not just history registration.** The fix migration deleted the 12 pre-launch test cases referencing the fakes and UPDATEd the 7 rows in place (same ids as the repo seed → repo and prod agree again; in-place also avoids the FK block, since `cases.care_home_id` has no ON DELETE rule). CSV columns beyond name/address (`federal_state`, `country`, `legal_name`, `operator_name`, `billing_*` — all billing values empty) deliberately not imported; revisit at the billing milestone.

**Seed-drift guard shipped:** `scripts/verify-baseline.mjs` extended from the 4 questionnaire tables to **all 9 seeded tables** (added `care_home`, `social_office`, `postal_code_rule`, `questionnaire`, `static_content`, plus `custom_prompt_de` to the group diff and >1000-row pagination). Run against prod after this pass: **all 9 tables identical to a fresh replay** — categories 9, groups 7, questions 158, options 546, care homes 7, social offices 376, PLZ rules 8159, questionnaires 2, static content 8. This class of drift can no longer hide.

**Verified live on prod (fresh case, full drive — all 10 points):** consent says Sorglos Antrag + infobox gone; exactly the 7 real homes, name-only; no Fall-ID, Status present; opening order exact (`Vorname → Geburtsname → Nachname → Geburtsdatum → Einzug → Straße → Stadt`), Geburtsname skippable, **no PLZ question anywhere**; year 22000 rejected + normal date accepted; Betreuer options `Nein | Gesetzlicher Betreuer | Bevollmächtigter Angehöriger | Beistandschaft`; children prompt exactly "Haben Sie weitere Kinder?"; locked banner shows the new copy (old copy absent) after completion; denominator **"0 von 57 Fragen beantwortet"** (58 − 1 required PLZ; optional birth_name adds 0). Test user deleted.

---

## M4 — Berlin content pass (Tiers 0–7) + engine fixes — ✅ complete 2026-07-05

The fourth milestone as executed. Numbering note: `architecture.md` §7's original plan listed "M4 — Documents", but document upload was descoped before launch, so the Berlin content pass took the M4 slot (M1 foundation/auth, M2 PLZ routing + engine, M3 chat UX — unchanged from §7).

Worked in numbered "tiers." Tier 0 was read-only diagnosis; Tiers 1–7 shipped. Each structural content tier used the same two-phase safety pattern (see reminders): a read-only Phase 1 report with explicit flags, then a Phase 2 migration only after the co-founder confirmed the plan.

### TIER 0 — Diagnostics (read-only)

Investigated three suspected problems in the questionnaire engine:

1. **Progress-bar denominator.** Confirmed it is computed _dynamically_, not hardcoded: `buildNav()` (`lib/questionnaire-nav.ts`) sets `totalRequired = flatVisible.filter(is_required).length`, where `flatVisible` is the set of currently-_visible_ questions. So the denominator correctly grows/shrinks as visibility changes. No bug in the denominator itself — but it's only as correct as the visibility logic feeding it (see Tier 1).
2. **Dependency re-evaluation.** The client recomputes `buildNav` via `useMemo` on every answer change, and `isVisible` is pure — so editing a trigger _does_ re-evaluate dependents. The originally-reported "dependents don't reappear" symptom traced to **data**, not the engine (a controller/typo mismatch already fixed in an earlier migration), plus the two real gaps fixed in Tier 1.
3. **Spouse visibility.** Found the real defect: several spouse dependents (`spouse_special_origin_rights_issued/_issued_by`, `spouse_life_insurance_amount`) were gated `value:"Ja"` on controllers that have **no "Ja" option** (their options are `Nein` + Ausweis/insurance types), so they could _never_ show. Root cause was two-fold — the wrong match value _and_ the engine's non-transitive visibility. Both addressed in Tier 1.

### TIER 1 — Engine bug fixes

Commit `ac6c4dc` (+ e2e verification `10917d1`). Files: `lib/questionnaire-nav.ts`, `lib/questionnaire-types.ts`, `app/case/actions.ts`, `app/case/chat-view.tsx`.

- **Transitive visibility (`isVisible`).** Previously `isVisible(rule, answers)` only checked whether the question's _own_ rule matched. Now it also requires the **controller named by that rule to itself be visible**, applied recursively up the whole chain (cycle-guarded). New signature `isVisible(rule, answers, rulesByKey?, seen?)`; `buildNav` builds a `question_key → rule` map via the new `buildRulesByKey()` and passes it to every call. The old one-level form is preserved for any caller that omits the map.
  - **General principle for all future content work:** a question is reachable only when **every** controller up its dependency chain is also satisfied — not just when its immediate rule value matches. When authoring a visibility rule, ask: _could the controller I reference itself be hidden?_ If yes, the child inherits that gating automatically now (which is what makes the Tier 1 spouse fix safe — see below).
- **Stale-answer clearing on hide.** When a saved answer changes such that previously-answered questions become hidden, `saveAnswerAction` (`app/case/actions.ts`) now deletes those now-unreachable answer rows (via `findStaleAnswerRefs()` over `buildNav().flatVisible`) so a later re-show of the trigger re-prompts from scratch instead of resurfacing stale data. It returns the cleared refs; `chat-view.tsx` applies them to local state (`applyClearedAnswers`). Runs only on an explicit save, never on load; the completion gate is unaffected (hidden questions were already out of the required count).
- **Spouse rule value fix** (migration `20260703000001`). Changed `spouse_special_origin_rights_issued`, `spouse_special_origin_rights_issued_by`, and `spouse_life_insurance_amount` from `value:"Ja"` → **`not_value:"Nein"`** (mirroring the working patient-side equivalents). This is only _safe_ because of the transitive-visibility fix: `not_value:"Nein"` would otherwise be true when the controller is `undefined` (i.e. hidden for a single applicant), but transitive visibility now also requires the marital-gated controller to be visible, so these stay correctly hidden for non-spouse cases.

### TIER 2 — Header/footer copy → DB + logo + migration-tracking repair

Commits `b86714e` (refactor), `80d30f6` (logo). Migration `20260703000002_static_content.sql`.

- **`static_content` table** (`key`, `value_de`, `updated_at`, RLS: authenticated read). Six header/footer strings moved out of hardcoded `lib/strings/de.ts` into it (honoring CLAUDE.md rule #2 "German text is data"): `brand.tagline`, `case.subheading`, `case.patient_banner_title`, `case.patient_banner_body`, `case.all_answered_heading`, `case.all_answered_message`. Loaded server-side via new `getStaticContent()` (`lib/dal.ts`) and threaded into `app/case/page.tsx` + `chat-view.tsx`. Missing keys degrade to `''` (never throws).
- **Logo.** Added `public/logo.jpg` (the "Sorglos Antrag" lockup) rendered via `next/image` in the brand header, replacing the placeholder box _and_ the duplicate wordmark text; tagline kept alongside.
- **Migration-tracking gap (important).** The `static_content` table was first created by hand in Studio (Claude Code can't run DDL). This revealed that **prod had no `supabase_migrations.schema_migrations` table at all** — i.e. _nothing_ had ever been tracked; the repo migrations were a reconstruction verified only by the baseline-replay script. Resolution: the co-founder linked the CLI and ran `supabase migration repair --status applied <version>` for **all 15** then-existing migrations, backfilling the tracking table. From that point on, `supabase db push` applies + tracks new migrations normally (used for Tiers 4–6).
- Also in this window: a separate **repo-wide Prettier PR** (`#1`, squash `c8c02a5`) — formatting only, no logic — to get `format:check` green.

### TIER 3 — Question-card category label removed + sticky header

Commit `f676951`. Files: `app/case/chat-view.tsx`, `app/case/page.tsx`.

- **Category label** no longer repeats on the active question card; the category now appears only as a section header in the answered/history view.
- **Sticky header.** Kept the codebase's app-shell pattern (flex column + inner scroll) but fixed the real mobile bug: shell height `h-screen` (100vh, breaks with the mobile address bar) → **`h-dvh`** (dynamic viewport), so the header stays pinned on mobile too. Progress bar stays visible; auto-scroll-to-latest still works; no z-index/overlap issues (flex regions don't overlap).
- **Mobile-specific decision:** the amber patient info-box is **pinned in the header on desktop** but on mobile it is **not pinned** — it renders once at the top of the scroll area and scrolls away — because pinning it left the mobile chat area cramped (~316px). Implemented with `hidden sm:block` (pinned copy) + `sm:hidden` (in-scroll copy).

### TIER 4 — Front-of-flow reorder + name split

Commit `1ca1944`. Migration `20260704000001_reorder_front_split_name.sql`.

- **Name split:** the single merged `name_pflegebedueftiger` ("Vollständiger Name…") was replaced by two required free-text questions **`first_name`** + **`last_name`** (ids `…f1`/`…f2`). The old question was deleted (its 10 pre-launch test answers cascade-deleted).
- **New front order of `antragsteller`:** `first_name → last_name → geburtsdatum → last_residence_plz → last_residence_street → last_residence_city → district_of_birth → …` (rest unchanged).
- **Address block moved:** `last_residence_plz/street/city` moved from `einkommen` into `antragsteller` (later moved again in Tier 5 — see below).
- **"Nursing home selection" is not a questionnaire question.** It's the **pre-questionnaire step** that sets `cases.care_home_id` (via `CareHomeSelector`), which runs before any question renders — so it needed no reorder. Likewise the _routing_ PLZ is the pre-questionnaire `PlzForm` (`cases.plz_before_move` → `resolvePlzAction` picks the questionnaire); the `last_residence_plz` _question_ is inert data (read by no code, controls no rule), so moving it has zero routing effect.
- Denominator went 59 → **60** here (one question became two).

### TIER 5 — Category restructuring

Commit `4aeb2fc`. Migration `20260704000002_tier5_categories.sql`.

- **New category `wohnsituation`** ("Wohnsituation") at `sort_order = 1` (right after `antragsteller`); subsequent categories renumbered contiguously. It contains the address block (`last_residence_plz`, `_street`, `_city`) — moved again out of `antragsteller` — plus **`apartment_ownership`** ("Was war das Mietverhältnis vor Heimeinzug?"), moved from `einkommen`. `wohnsituation` sits _before_ `einkommen` deliberately: `apartment_ownership` controls 5 rent-detail questions still in `einkommen`, so the controller must precede its dependents in flow order.
- **`dokumente` renamed in place to `kinder`** ("Kinder") — same category id `…0003`, so its 8 `child_*` questions and the `children` group followed automatically (no reassignment needed). (The old "Erforderliche Dokumente" category held only the child questions after the earlier document-upload removal.)
- **Deleted the duplicate marital-status question `familienstand`** ("Familienstand der pflegebedürftigen Person", `…0003` question id). It controlled nothing; 9 pre-launch test answers cascade-deleted. The **kept** question is `marital_status` ("Was ist Ihr Familienstand?") — it stays at its Tier-4 front position in `antragsteller` and controls **30 dependents** (the entire spouse section). `familienstand`'s options were a strict subset of `marital_status`'s, confirming which to keep.
- Denominator went 60 → **59** (one deletion).

### TIER 6 — Repeatable groups

Commit `6605c86`. Migration `20260704000003_tier6_additional_wealth_repeatable.sql`.

- **Key finding:** three of the four target sections were _already_ repeatable groups (built during M3/earlier group work but not documented as such): **`pension`**, **`other_income`**, **`bank_additional`** all have `is_repeatable = true` and loop via the existing "add another?" prompt. No change needed.
- **Only `additional_wealth` ("Besitzen Sie weitere Vermögenswerte?") was still flat.** Converted it to a repeatable group mirroring `bank_additional`: new `question_group` `additional_wealth` (uncapped), with `additional_wealth_type` + `additional_wealth_amount` as members (their `{value:"Ja", question_key:"additional_wealth_yes_no"}` visibility kept; `additional_wealth_yes_no` stays the flat trigger). 8 stale `group_instance='default'` test answers dropped.
- **How the group mechanism works** (for future reference): instances are _not_ rows — each `answer` carries a `group_instance` UUID (or `'default'`). `deriveGroupData` (`page.tsx`) rebuilds `groupInstances`/`groupAnswers` from answers and auto-creates one instance for empty repeatable groups. `buildNav` expands each instance and emits a `groupPrompt` once a group's visible questions are all answered (and it's under `max_count`, `NULL`=uncapped). `chat-view` renders the "Möchten Sie eine weitere {group} hinzufügen?" card (`Ja, hinzufügen` / `Nein, weiter`); "Ja" appends a new instance UUID. History shows a header per instance ("{group} {n}").
- Pension cap decision: kept **uncapped** (consistent with the other groups; a cap is arbitrary and easy to add later if ever needed).
- Verified live: answering the assets trigger "Ja" then the loop prompt produced two instances ("Weitere Vermögenswerte 1" and "…2").

### TIER 7 — Final content pass

Commits `3d20f08` (migration) + `7af33c3` (code). Migration `20260705000001_tier7_content_pass.sql`. Closed out all eight open items from Tiers 0–6 in one bundle:

1. **`country_of_birth` → dropdown.** `single_select` with 195 countries (Deutschland first, rest alphabetical) and `validation.default = "Deutschland"`. The default is consumed by a new general mechanism: `initialValueFor()` in `chat-view.tsx` pre-fills the draft from `validation.default`, and since `handleSave` submits `currentValue`, an untouched "Weiter" saves the default. Reusable for any future data-driven default.
2. **Betreuer/Beistand.** `power_of_attorney` now offers exactly `Gesetzlicher Betreuer` / `Bevollmächtigter Angehöriger` / `Beistandschaft` and is **optional** (`is_required = false` — this is what moved the denominator 59 → 58). Old-value test answers dropped. "No selection" flows through the skip button ("Weiß ich gerade nicht"): a plain Weiter with nothing selected saves `''` but `isAnswered` treats `''` as unanswered, so the question re-asks.
3. **Year dropdowns + district link.** `berlin_since` and `berlin_district_since` became `single_select` year pickers (2026 → 1930, newest first; no new answer_type) and moved into `wohnsituation` right after the address block: `PLZ → Straße → Stadt → seit wann Berlin → seit wann Bezirk → Mietverhältnis`. The district link is expressed by physical ordering (no group/visibility mechanism). Old free-text answers dropped.
4. **Mietfrei.** `apartment_ownership` gained "Mietfrei bei Freunden/Familie" as 4th option; the 5 `Mietwohnung`-gated rent questions are unaffected.
5. **Insurance reorder.** All **three** wealth-resident life-insurance follow-ups (`total_amount`, `name`, `number` — the task named two, but `total_amount` sat beside them gated on the same trigger) moved into `expenditure` so=8/9/10, directly after the trigger and its monthly-amount (so=6/7). Visibility rules unchanged; none are controllers.
6. **IBAN copy.** `bank_savings_iban` → "Was ist Ihre IBAN Nummer Ihres Sparkontos?" (was identical to the giro IBAN prompt).
7. **Pension "none" option.** `pension_type` gained "Keine Rente", and the three follow-ups (`pension_amount/id/issuer`) switched from `not_empty` to `in_values:[8 real pension types]` so "Keine Rente" doesn't force an entry. Deliberately no yes/no gate.
8. **Loop-prompt wording.** New nullable `question_group.custom_prompt_de` column, threaded loader → nav (`GroupPromptInfo.customPromptDe`) → `GroupPromptCard` (falls back to the `{group}` template when NULL). Roman's four exact prompts set for `pension`, `other_income`, `bank_additional`, `additional_wealth`. Content stays in the DB per convention.

Scope decisions: **main-applicant only** (spouse mirrors intentionally untouched); all-three insurance follow-ups moved; year dropdown over free number input; pre-select via `validation.default`.

**Deploy-order lesson:** the new code selects the new `custom_prompt_de` column, so the migration had to be applied **before** pushing code to `main` (old code ignores the column — safe window; new code against old schema would break the loader). A read-only DB check script gated the push — and caught that the migration hadn't actually been applied on the first "confirmed" attempt.

Verified live on prod (fresh case, full drive): Deutschland pre-selected untouched; the 3 Betreuer options with none pre-selected + skip path; both year pickers (2026→1930) in the right position; Mietfrei present; the 5 insurance prompts consecutive; exact IBAN copy; "Keine Rente" suppresses follow-ups while "Altersrente" shows them; all four custom prompts captured verbatim and `children` still uses the template. Test user deleted.

---

## M4 leftovers / backlog (updated after feedback pass 2)

Carried forward explicitly so nothing gets lost. ~~Done in feedback pass 2~~: the `children` loop prompt (now "Haben Sie weitere Kinder?") and the Betreuer/Beistand product question (resolved with the explicit "Nein" option — "confirmed: kein Betreuer" is now a stored answer).

- **Loop-prompt wording for the spouse groups** — `spouse_pension`, `spouse_other_income` still use the template. Needs wording from Roman, then one `custom_prompt_de` UPDATE each.
- **"Keine Rente" quirk** — after selecting it, the pension group's loop prompt ("Möchten Sie weitere Renten hinzufügen?") still fires, because the group's only visible question is answered. Harmless (one "Nein, weiter" click) but slightly odd; suppressing it would need a new engine rule. Flagged, not fixed.
- **Spouse mirrors** — `spouse_country_of_birth` (still free text) and `spouse_power_of_attorney` (still old options, required, no "Nein") were deliberately excluded from Tier 7 and feedback pass 2; revisit if spouse-section parity is wanted.
- **Optional-question UX** — Weiter with nothing selected saves `''` and re-asks (only the skip button "Weiß ich gerade nicht" truly moves on). Mitigated for the Betreuer question by its new "Nein" option; the generic behavior remains for `birth_name` and any future optional question.
- **Dormant CI e2e job** — the Playwright job in `.github/workflows/ci.yml` is gated on the `NEXT_PUBLIC_SUPABASE_URL` repository variable, which has never been set, so it is skipped on every run (only lint/format/typecheck/unit actually gate pushes). Set the variable plus the `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` / `SUPABASE_SECRET_KEY` secrets to activate it; until then, verification is the ad-hoc prod drives described per tier.
- **Care-home structured columns / billing** — the CSV's `federal_state`, `legal_name`, `operator_name`, `billing_*` columns are not in the `care_home` schema (name/address/is_active only). Extend at the billing milestone; source data: the CSV noted in the feedback-pass-2 migration comment.

---

## Project-wide reminders (restate every session)

1. **Migrations only — no dashboard/Studio content edits.** All content/schema changes go through dated `supabase/migrations/*.sql` applied with `supabase db push`. If Studio is _ever_ used for something urgent, **immediately** backfill the migration history (`supabase migration repair --status applied <version>`) — the Tier 2 discovery showed that skipping this leaves prod untracked and creates repo↔DB drift. **And repair alone is not enough:** it stamps history without verifying content (the feedback-pass-2 care-home drift hid behind a repaired-but-never-run seed). After any backfill, run `scripts/verify-baseline.mjs` against a fresh local replay to prove prod content matches the migrations. Migrations are the single source of truth (CLAUDE.md #8).
2. **Transitive-visibility principle (from Tier 1).** Any new/edited `visibility_rule` must consider whether its referenced _controller could itself be hidden_, not just whether the immediate value matches. `isVisible` now enforces this recursively, so authoring `not_value:"Nein"` on a controller that can be `undefined`/hidden is safe _only_ because the chain is also checked — reason about the whole chain.
3. **Two-phase pattern for structural changes.** For anything touching ordering, categories, question IDs, or deletions: do a **Phase 1 read-only report with explicit flags** for every ambiguous call, get human confirmation, then write the **Phase 2 migration**. Don't skip it even when a change looks trivial — Tiers 4/5/6 each surfaced a non-obvious issue (e.g. "nursing home" isn't a question; the address block spanned two categories; three of four "repeatable" sections were already done; a controller-before-dependents ordering constraint). Confirm answer counts before any cascade delete (pre-launch test data only so far).
4. **Verification is live.** Prove content/engine changes against prod with Playwright (MCP or the e2e adaptive-loop runner): check flow order, that expected controllers still gate their dependents, and that the progress denominator changes by exactly the expected amount. Create throwaway test users via the admin API and delete them afterward.
5. **REAL-DATA RULE (since the pilot, 2026-07-22).** Real users exist. Every destructive migration's Phase 1 must report impact split by test vs. real accounts (row counts per affected object, identifying non-test owners) before the founder approves the push. Test accounts are identifiable by `@hzp-test.invalid` / `pw-*` / `verif+*` patterns; treat anything else as real. **Standing precedent: a destructive migration touching real accounts requires `case:export` snapshots of every affected case BEFORE the push.** **Sibling precedent — ADDITIVE BACKFILL (2026-07-23):** a structural change that would hide or invalidate real completed cases' existing data (e.g. a new gate over already-answered questions) must backfill the consistent answer for the affected real cases in the same migration, via guarded idempotent INSERTs that no-op on local replay.
